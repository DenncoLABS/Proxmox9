#!/usr/bin/env bash
set -euo pipefail

REPAIR_CMD="/usr/sbin/dennco-novnc-clipboard-repair"

if [[ -x "$REPAIR_CMD" ]]; then
  "$REPAIR_CMD"
else
  echo "Repair command not found: $REPAIR_CMD"
  exit 1
fi

echo "Dennco noVNC clipboard panel installed or repaired."
